spring.application.name=configuration-service
server.port = 8888

#to use file systems instead of local git repository
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=classpath:config/